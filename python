# 1. Go back to your agent directory
cd ~/git/summarizeragent_agent/app/agent

# 2. Add the newly built wheel file (using the absolute path to make it easy)
uv add ~/git/base_agent/dist/base_agent-0.1.0-py3-none-any.whl

# 3. Start your agent!
uv run main.py
